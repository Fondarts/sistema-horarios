rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permitir acceso a archivos de certificados médicos
    match /absence-certificates/{employeeId}/{fileName} {
      // Permitir lectura y escritura para usuarios autenticados
      allow read, write: if request.auth != null;
    }
    
    // Permitir acceso a otros archivos del sistema
    match /{allPaths=**} {
      // Permitir lectura y escritura para usuarios autenticados
      allow read, write: if request.auth != null;
    }
  }
}